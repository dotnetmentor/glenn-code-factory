/* THIS (.ts) FILE IS GENERATED BY TypedSignalR.Client.TypeScript */
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import type { IStreamResult, Subject } from '@microsoft/signalr';
import type { SubmitPromptPayload, SubmitPromptResponse, CancelTurnRequest, EventReplayRequest, AgentEventNotification, ResolvePermissionPayload, HeartbeatPayload, AgentSecretsDto, ErrorReportPayload, DiskPressurePayload, EmitEventPayload, ReportSessionCostPayload, PermissionRequestedPayload, RuntimeEventPayloadDto, ServiceLogLineDto, DaemonLogLineDto, LiveSupervisordSnapshotPayload, RuntimeStateChangedNotification, BootstrapProgressNotification, RuntimeWakingNotification, NotificationPayload, RunResultNotification, ConversationRenamedNotification, RuntimeDiskPressureNotification, RuntimeEventNotification, ServiceLogLineNotification, LiveSupervisordSnapshotNotification, DaemonLogLineNotification, PreviewPortChangedNotification, AttachmentStateChangedPayload, SpecificationChangedNotification, CardChangedNotification, SubtaskChangedNotification, StartTurnPayload, CancelTurnPayload, ConfigUpdatePayload, RestartServicePayload, ForceRebootstrapPayload } from '../Source.Features.SignalR.Contracts';
import type { BootstrapPayloadV2, RepoAccessToken } from '../Source.Features.RuntimeBootstrap.Contracts';
import type { AgentPermissionsConfig } from '../Source.Features.AgentPermissions.Models';
import type { TurnRefusedPayload } from '../Source.Features.Conversations.Models';
import type { RuntimeSpecDeltaApplyResultPayload, RuntimeProposalCreatedPayload, RuntimeProposalUpdatedPayload, ApplyRuntimeSpecDeltaPayload } from '../Source.Features.RuntimeCuration.Models';
import type { HookStartedPayload, HookProgressPayload, HookCompletedPayload, HookConfigErrorPayload, HookSelfHealStartedPayload, HookSelfHealMaxedOutPayload, RequestSelfHealContinuationPayload, RequestSelfHealContinuationResponse } from '../Source.Features.Hooks.Models';
import type { GitOperationStartedPayload, GitOperationCompletedPayload, CommitMadePayload, GitPushFailedPayload, GitPushSucceededPayload, CommitFailedPayload, MergeConflictPayload, RequestDestructiveGitOpPayload, RequestDestructiveGitOpResponse, MergeBranchPayload } from '../Source.Features.GitOps.Models';
import type { ChangedFilesRequest, ChangedFilesResponse, FileDiffRequest, FileDiffResponse, CommitRangeResponse } from '../Source.Features.Diffs';

/**
* Strongly-typed server method surface for  — the methods
* React clients are allowed to invoke. Decorated with [Hub] so the
* TypedSignalR.Client.TypeScript analyzer emits a real
* HubProxyFactoryProvider entry; without it the generated index.ts ships
* an empty stub and the React side gets undefined when it asks for a
* proxy.
* 
* The matching  receiver interface is the
* server-to-React push channel. Two interfaces, two directions, one hub.Lifecycle hooks (OnConnectedAsync, OnDisconnectedAsync) are
* SignalR-framework concerns and are not declared here.
*/
export type IAgentHub = {
    /**
    * @param payload Transpiled from Source.Features.SignalR.Contracts.SubmitPromptPayload
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.SignalR.Contracts.SubmitPromptResponse>
    */
    submitPrompt(payload: SubmitPromptPayload): Promise<SubmitPromptResponse>;
    /**
    * @param payload Transpiled from Source.Features.SignalR.Contracts.CancelTurnRequest
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    cancelTurn(payload: CancelTurnRequest): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.SignalR.Contracts.EventReplayRequest
    * @returns Transpiled from System.Threading.Tasks.Task<System.Collections.Generic.List<Source.Features.SignalR.Contracts.AgentEventNotification>>
    */
    requestEventReplay(payload: EventReplayRequest): Promise<AgentEventNotification[]>;
    /**
    * Subscribe this connection to the workspace-{workspaceId} SignalR
    * group so the sidebar receives cross-project broadcasts (runtime state,
    * turn lifecycle, conversation rename) for every project in the workspace,
    * not just the one in the current tab. Returns once the membership check
    * succeeds and the group join is committed; throws HubException if
    * the caller is not a member of that workspace.
    * @param workspaceId Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    joinWorkspace(workspaceId: string): Promise<void>;
    /**
    * Symmetric counterpart to : removes this
    * connection from the workspace-{workspaceId} group. Idempotent —
    * safe to call even if the connection isn't currently a member.
    * @param workspaceId Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    leaveWorkspace(workspaceId: string): Promise<void>;
    /**
    * React-to-server invocation: the user clicked one of the four actions on
    * an inline approval card and the daemon's pending canUseTool
    * callback needs the answer. The hub resolves
    * project → active runtime → daemon connection and forwards via
    * , correlated by
    * ToolUseId. Pure relay, no persistence — session-scoped wire only.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ResolvePermissionPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    resolvePermission(payload: ResolvePermissionPayload): Promise<void>;
    /**
    * Subscribe this connection to the runtime-events:{runtimeId}
    * SignalR group so the runtime drawer's Timeline tab receives live
    * pushes for every event the daemon emits. The hub verifies the caller
    * has access to the runtime (the runtime row must exist and resolve to
    * a project the user can reach — see implementation for the current
    * gate). Idempotent — re-joining a group is a SignalR no-op.
    * @param runtimeId Transpiled from System.Guid
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    subscribeToRuntimeEvents(runtimeId: string): Promise<void>;
    /**
    * Symmetric counterpart to :
    * removes this connection from the runtime-events:{runtimeId}
    * group when the drawer's Timeline tab closes. Idempotent — SignalR's
    * RemoveFromGroupAsync is a no-op for non-members and no auth
    * check is performed (leaving a group you may already have left is
    * harmless).
    * @param runtimeId Transpiled from System.Guid
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    unsubscribeFromRuntimeEvents(runtimeId: string): Promise<void>;
    /**
    * Subscribe this connection to the service-logs:{runtimeId}:{serviceName}
    * SignalR group so the runtime drawer's Logs tab receives every line
    * the daemon's tail -F reads off the supervised service's log
    * file. The hub validates the caller has access to the runtime AND
    * the supplied service name is declared in the current
    * ProjectRuntime.Spec — undeclared service names are refused
    * silently so a curious frontend can't drive the daemon to spawn
    * arbitrary tail processes. The first subscriber on a given
    * (runtimeId, serviceName) pair causes the hub to push
    * to the daemon; subsequent
    * subscribers ride on the daemon's LogTailer reference counter
    * without a second push.
    * @param runtimeId Transpiled from System.Guid
    * @param serviceName Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    subscribeToServiceLogs(runtimeId: string, serviceName: string): Promise<void>;
    /**
    * Symmetric counterpart to :
    * removes this connection from the
    * service-logs:{runtimeId}:{serviceName} group when the Logs
    * tab closes. The hub pushes
    * to the daemon on every call — the daemon's reference counter
    * debounces it down to a single SIGTERM when the last subscriber
    * detaches. Idempotent and unauthenticated (leaving a group you may
    * already have left is harmless).
    * @param runtimeId Transpiled from System.Guid
    * @param serviceName Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    unsubscribeFromServiceLogs(runtimeId: string, serviceName: string): Promise<void>;
    /**
    * runtime-observability-super-admin — super-admin only. Subscribe this
    * connection to the daemon-logs:{runtimeId} SignalR group so the
    * runtime drawer's Daemon Logs tab receives every line the daemon's
    * tail -F reads off
    * /var/log/supervisor/agent.out.log + agent.err.log. The
    * hub gates the call on the global super-admin claim (the daemon's own
    * stdout/stderr can carry operator-sensitive bootstrap detail) and pushes
    * to the daemon on the
    * first subscriber. Subsequent subscribers ride on the daemon's
    * reference counter without a second push.
    * @param runtimeId Transpiled from System.Guid
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    subscribeToDaemonLogs(runtimeId: string): Promise<void>;
    /**
    * Symmetric counterpart to : removes
    * this connection from the daemon-logs:{runtimeId} group when the
    * drawer's Daemon Logs tab closes. The hub pushes
    * to the daemon on every
    * call — the daemon's reference counter debounces it down to a single
    * SIGTERM when the last subscriber detaches. Idempotent and
    * unauthenticated (leaving a group you may already have left is
    * harmless).
    * @param runtimeId Transpiled from System.Guid
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    unsubscribeFromDaemonLogs(runtimeId: string): Promise<void>;
}

/**
* Strongly-typed server method surface for  — the
* methods React clients are allowed to invoke. The matching
* receiver interface is the server-to-React push
* channel. Two interfaces, two directions, one hub — same convention as
* + .
* 
* The hub is subscribe-only on the inbound side: a tab joins / leaves
* the projects it cares about. There is no SubmitX entry point on this
* hub — domain mutations happen through the REST controllers (Orval mutations
* on the client), and the resulting domain events fan out via
* push methods.Lifecycle hooks (OnConnectedAsync, OnDisconnectedAsync)
* are SignalR-framework concerns and intentionally not declared here.
*/
export type IPlanningHub = {
    /**
    * Subscribe this connection to the project:{projectId} SignalR
    * group so the client receives every planning broadcast
    * (,
    * ,
    * ) for that project.
    * Called by the frontend on project board / spec list mount. Idempotent —
    * re-joining a group is a SignalR no-op.
    * @param projectId Transpiled from System.Guid
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    joinProject(projectId: string): Promise<void>;
    /**
    * Symmetric counterpart to : removes this
    * connection from the project:{projectId} group when the planning
    * surface unmounts. Idempotent — SignalR's RemoveFromGroupAsync
    * is a no-op for non-members.
    * @param projectId Transpiled from System.Guid
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    leaveProject(projectId: string): Promise<void>;
}

/**
* Strongly-typed server method surface for  — the methods
* the daemon (TypedSignalR-generated TS client) is allowed to invoke. Carrying
* the contract on a single [Hub]-decorated interface drives the
* TypedSignalR.Client.TypeScript analyzer to emit a non-empty
* HubProxyFactoryProvider; without it the generator ships a stub and the
* daemon side has nothing to call.
* 
* The matching  receiver interface lives next
* door — that is the server-to-daemon push channel. Two interfaces, two
* directions, one hub.Lifecycle hooks (OnConnectedAsync, OnDisconnectedAsync) are
* SignalR-framework concerns and intentionally not declared here.
*/
export type IRuntimeHub = {
    /**
    * @param payload Transpiled from Source.Features.SignalR.Contracts.HeartbeatPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    heartbeat(payload: HeartbeatPayload): Promise<void>;
    /**
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.RuntimeBootstrap.Contracts.BootstrapPayloadV2>
    */
    getBootstrap(): Promise<BootstrapPayloadV2>;
    /**
    * Daemon-invoked: fetch the per-prompt Anthropic credentials for the
    * project this connection's runtime is pinned to. The fall-back chain
    * is implemented server-side; daemon just consumes the result.
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.SignalR.Contracts.AgentSecretsDto>
    */
    getSecrets(): Promise<AgentSecretsDto>;
    /**
    * Daemon-invoked: fetch the effective permission config for the project this
    * connection's runtime is pinned to. Resolution is "project override or system
    * defaults" (no merging) — see . The
    * daemon caches the result for the duration of a single turn so mid-turn
    * config changes don't create inconsistent decisions inside one tool sequence.
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.AgentPermissions.Models.AgentPermissionsConfig>
    */
    getAgentPermissions(): Promise<AgentPermissionsConfig>;
    /**
    * Daemon-invoked: mint a fresh, repository-scoped GitHub-App installation
    * token for cloning the project's repo over HTTPS. Replaces the legacy
    * deploy-key+SSH path. The daemon must pass the owner/name of the
    * repo it intends to clone; the server cross-checks this against the
    * connection's pinned project and rejects mismatches (a compromised
    * daemon cannot mint tokens for arbitrary repos in the same installation).
    * @param repoFullName Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.RuntimeBootstrap.Contracts.RepoAccessToken>
    */
    getRepoAccessToken(repoFullName: string): Promise<RepoAccessToken>;
    /**
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeReady(): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ErrorReportPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    reportError(payload: ErrorReportPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.SignalR.Contracts.DiskPressurePayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    reportDiskPressure(payload: DiskPressurePayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.SignalR.Contracts.EmitEventPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    emitEvent(payload: EmitEventPayload): Promise<void>;
    /**
    * Daemon-to-server per-message cost report (legacy Claude path). Fires
    * from the daemon's onRawMessage hook once per assistant message
    * that carries a usage block. The hub accumulates the
    * per-message tokens + derived USD cost onto the addressed
    * , so the running total at session end is
    * the sum across every assistant message in that turn.
    * 
    * The first two positional arguments (containerId,
    * sessionId) match the wire shape the deployed
    * glenn-code daemon already invokes — adding the hub method
    * alone makes its existing emissions land instead of failing silently
    * in the daemon's .catch(console.error). The containerId
    * is observability-only; auth + routing run off the connection-bound
    * RuntimeId claim like every other daemon push method.
    * @param containerId Transpiled from string
    * @param sessionId Transpiled from System.Guid
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ReportSessionCostPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    reportSessionCost(containerId: string, sessionId: string, payload: ReportSessionCostPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Conversations.Models.TurnRefusedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    turnRefused(payload: TurnRefusedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.RuntimeCuration.Models.RuntimeSpecDeltaApplyResultPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeSpecDeltaApplied(payload: RuntimeSpecDeltaApplyResultPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Hooks.Models.HookStartedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookStarted(payload: HookStartedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Hooks.Models.HookProgressPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookProgress(payload: HookProgressPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Hooks.Models.HookCompletedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookCompleted(payload: HookCompletedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Hooks.Models.HookConfigErrorPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookConfigError(payload: HookConfigErrorPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Hooks.Models.HookSelfHealStartedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookSelfHealStarted(payload: HookSelfHealStartedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Hooks.Models.HookSelfHealMaxedOutPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookSelfHealMaxedOut(payload: HookSelfHealMaxedOutPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.Hooks.Models.RequestSelfHealContinuationPayload
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.Hooks.Models.RequestSelfHealContinuationResponse>
    */
    requestSelfHealContinuation(payload: RequestSelfHealContinuationPayload): Promise<RequestSelfHealContinuationResponse>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.GitOperationStartedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitOperationStarted(payload: GitOperationStartedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.GitOperationCompletedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitOperationCompleted(payload: GitOperationCompletedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.CommitMadePayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    commitMade(payload: CommitMadePayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.GitPushFailedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitPushFailed(payload: GitPushFailedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.GitPushSucceededPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitPushSucceeded(payload: GitPushSucceededPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.CommitFailedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    commitFailed(payload: CommitFailedPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.MergeConflictPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    mergeConflict(payload: MergeConflictPayload): Promise<void>;
    /**
    * @param payload Transpiled from Source.Features.GitOps.Models.RequestDestructiveGitOpPayload
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.GitOps.Models.RequestDestructiveGitOpResponse>
    */
    requestDestructiveGitOp(payload: RequestDestructiveGitOpPayload): Promise<RequestDestructiveGitOpResponse>;
    /**
    * Daemon-to-server: the agent SDK's canUseTool callback fired and
    * the daemon wants the user to make the call. Relay-only — the hub fans
    * the payload out to every React tab in the project-{projectId}
    * group via . The eventual
    * AgentHub.ResolvePermission ships the decision back through
    * , correlated by the
    * payload's ToolUseId.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.PermissionRequestedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    permissionRequested(payload: PermissionRequestedPayload): Promise<void>;
    /**
    * Daemon-to-server: append a structured event to this runtime's event
    * store and broadcast it to subscribed frontends. The owning runtime
    * is resolved server-side from the connection's signed rt_runtime
    * claim — the daemon does not (and cannot) supply a RuntimeId on
    * the wire. After persistence the hub fans the event out to the
    * runtime-events:{runtimeId} group via
    * so open drawer
    * Timeline tabs see it in real time.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.RuntimeEventPayloadDto
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    recordRuntimeEvent(payload: RuntimeEventPayloadDto): Promise<void>;
    /**
    * Daemon-to-server: a single line of stdout/stderr from a service the
    * daemon's LogTailer is currently tailing. The hub resolves the
    * owning RuntimeId from the connection's signed rt_runtime
    * claim, then broadcasts to the
    * service-logs:{runtimeId}:{serviceName} group via
    * . Lines are NOT persisted —
    * the supervisord-rotated file on disk is the only durable copy.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ServiceLogLineDto
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    serviceLogLine(payload: ServiceLogLineDto): Promise<void>;
    /**
    * runtime-observability-super-admin — daemon→server: a single line of
    * stdout/stderr from the daemon's own log files
    * (/var/log/supervisor/agent.out.log,
    * /var/log/supervisor/agent.err.log). The hub resolves the owning
    * RuntimeId from the connection's signed claim, then broadcasts to
    * the daemon-logs:{runtimeId} group via
    * . Lines are NOT
    * persisted — the supervisord-rotated file is the durable source.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.DaemonLogLineDto
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    daemonLogLine(payload: DaemonLogLineDto): Promise<void>;
    /**
    * Daemon-to-server live supervisord snapshot push. Pushed by the daemon's
    * ServiceStatusPoller on every poll tick (default 10s). The hub
    * resolves the owning RuntimeId from the connection's signed
    * rt_runtime claim — the daemon does not supply it on the wire —
    * then fans the payload out to the runtime-events:{runtimeId}
    * group via .
    * 
    * Not persisted. Pure push-through — consumers cache the
    * latest snapshot keyed by runtimeId.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.LiveSupervisordSnapshotPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    pushLiveSupervisordSnapshot(payload: LiveSupervisordSnapshotPayload): Promise<void>;
}

/**
* Strongly-typed receiver interface for clients connected to .
* Every method here is invoked by the server on the React client; the React side
* registers handlers for these names. Keeping the contract on a single interface
* means a backend change that drops a method breaks compile in the generated
* TypeScript — which is the whole point of using TypedSignalR.
* 
* This card defines only the receiver shape. The server-side methods that
* invoke these (e.g. domain-event-to-broadcast handlers) ship in subsequent
* cards of the signalr-architecture spec.
*/
export type IAgentClient = {
    /**
    * Pushed whenever a runtime moves between lifecycle states.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.RuntimeStateChangedNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeStateChanged(payload: RuntimeStateChangedNotification): Promise<void>;
    /**
    * Pushed during daemon bootstrap to drive the boot-progress UI.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.BootstrapProgressNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    bootstrapProgress(payload: BootstrapProgressNotification): Promise<void>;
    /**
    * Pushed by the wake-on-connect flow the moment we ask Fly to start a
    * suspended machine — gives the chat panel a "waking up..." affordance
    * immediately, before the slower
    * Suspended → Waking edge
    * fans out from the state-machine transition.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.RuntimeWakingNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeWaking(payload: RuntimeWakingNotification): Promise<void>;
    /**
    * Generic toast / banner notification surface.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.NotificationPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    notification(payload: NotificationPayload): Promise<void>;
    /**
    * Pushed for every AgentEvent the daemon emits into a session
    * (assistant text chunks, tool calls, lifecycle ticks, …). Drives the
    * live conversation feed in the React client. Replay of missed events on
    * reconnect is handled by a separate request/response flow, not by this
    * fire-and-forget broadcast.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.AgentEventNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    agentEvent(payload: AgentEventNotification): Promise<void>;
    /**
    * Pushed when a session's terminal Status frame lands and the daemon
    * included a  aggregate. Drives the chat
    * panel's turn footer ("Finished in 14.2s · claude-sonnet-4 · 5 files
    * edited · view PR ↗") live without a REST round-trip. Fan-out scope
    * mirrors  — same branch + workspace groups.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.RunResultNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runResult(payload: RunResultNotification): Promise<void>;
    /**
    * Pushed whenever a conversation's title changes — user-initiated rename
    * or one-shot auto-retitle off the first AssistantText chunk. Drives live
    * title updates in the conversation list and chat header across all open
    * tabs of the same project.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ConversationRenamedNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    conversationRenamed(payload: ConversationRenamedNotification): Promise<void>;
    /**
    * Pushed when a hook process starts on a daemon. Drives the
    * "running…" affordance in the chat panel's hook strip.
    * @param payload Transpiled from Source.Features.Hooks.Models.HookStartedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookStarted(payload: HookStartedPayload): Promise<void>;
    /**
    * Pushed for every stdout line of a still-running hook. Not
    * persisted server-side — pure live UX.
    * @param payload Transpiled from Source.Features.Hooks.Models.HookProgressPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookProgress(payload: HookProgressPayload): Promise<void>;
    /**
    * Pushed when a hook process exits (any exit code). Carries the
    * 16 KiB tail and the SHA-256 of the full output for dedupe.
    * @param payload Transpiled from Source.Features.Hooks.Models.HookCompletedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookCompleted(payload: HookCompletedPayload): Promise<void>;
    /**
    * Pushed when a hook could not run at all (command not found,
    * malformed config, sandbox refusal, …). Distinct from a hook that ran
    * and failed.
    * @param payload Transpiled from Source.Features.Hooks.Models.HookConfigErrorPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookConfigError(payload: HookConfigErrorPayload): Promise<void>;
    /**
    * Relay-only: the daemon has started another turn to recover
    * from a failing hook. UX hint; no persistence.
    * @param payload Transpiled from Source.Features.Hooks.Models.HookSelfHealStartedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookSelfHealStarted(payload: HookSelfHealStartedPayload): Promise<void>;
    /**
    * Relay-only: the daemon has exhausted the self-heal budget.
    * UX hint; no persistence.
    * @param payload Transpiled from Source.Features.Hooks.Models.HookSelfHealMaxedOutPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    hookSelfHealMaxedOut(payload: HookSelfHealMaxedOutPayload): Promise<void>;
    /**
    * Pushed when the daemon begins a git operation. Drives the
    * "running…" affordance in the git activity strip.
    * @param payload Transpiled from Source.Features.GitOps.Models.GitOperationStartedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitOperationStarted(payload: GitOperationStartedPayload): Promise<void>;
    /**
    * Pushed when a git operation exits. Carries the exit code,
    * duration, and a 16 KiB output tail for the UI.
    * @param payload Transpiled from Source.Features.GitOps.Models.GitOperationCompletedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitOperationCompleted(payload: GitOperationCompletedPayload): Promise<void>;
    /**
    * Pushed when a commit lands. High-level UX signal so the chat
    * panel and commit history can refresh without parsing every git op.
    * @param payload Transpiled from Source.Features.GitOps.Models.CommitMadePayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    commitMade(payload: CommitMadePayload): Promise<void>;
    /**
    * Pushed when a git push fails. Carries a coarse failure
    * reason for branching + an output tail for operators.
    * @param payload Transpiled from Source.Features.GitOps.Models.GitPushFailedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitPushFailed(payload: GitPushFailedPayload): Promise<void>;
    /**
    * Pushed when a git push succeeds. Positive ack so the UI
    * can clear an "out of sync with GitHub" banner once the working tree is
    * back in lockstep with the remote.
    * @param payload Transpiled from Source.Features.GitOps.Models.GitPushSucceededPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    gitPushSucceeded(payload: GitPushSucceededPayload): Promise<void>;
    /**
    * Pushed when a git commit attempt failed (e.g. missing
    * identity, hook rejection, lock contention). Drives the "out of sync
    * with GitHub" banner alongside .
    * @param payload Transpiled from Source.Features.GitOps.Models.CommitFailedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    commitFailed(payload: CommitFailedPayload): Promise<void>;
    /**
    * Pushed when a merge produced conflicts. Fan-out only; the
    * working tree is the source of truth.
    * @param payload Transpiled from Source.Features.GitOps.Models.MergeConflictPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    mergeConflict(payload: MergeConflictPayload): Promise<void>;
    /**
    * Pushed when the daemon requested an approval for a destructive
    * git op. The UI surfaces the prompt; the eventual approve/reject flow
    * ships in a follow-up card.
    * @param approvalId Transpiled from System.Guid
    * @param payload Transpiled from Source.Features.GitOps.Models.RequestDestructiveGitOpPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    destructiveGitOpRequested(approvalId: string, payload: RequestDestructiveGitOpPayload): Promise<void>;
    /**
    * Pushed when the daemon refused to start a turn because another
    * turn is still in flight on the same runtime (single-turn invariant).
    * The rejected session has been flipped to
    * server-side; this fan-out lets
    * the chat panel surface the refusal without polling.
    * @param payload Transpiled from Source.Features.Conversations.Models.TurnRefusedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    turnRefused(payload: TurnRefusedPayload): Promise<void>;
    /**
    * Pushed when the daemon successfully calls
    * POST /api/runtimes/{id}/proposals (i.e. invokes its
    * propose_runtime_spec custom tool). Drives the confirmation-card
    * UI in the chat panel — Approve / Edit / Reject is rendered directly
    * from this notification.
    * @param payload Transpiled from Source.Features.RuntimeCuration.Models.RuntimeProposalCreatedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeProposalCreated(payload: RuntimeProposalCreatedPayload): Promise<void>;
    /**
    * Pushed every time a  changes status — user
    * decision (Approved / Edited / Rejected) and daemon ack (Applied /
    * Failed). Lets the chat panel morph the pending confirmation card into
    * its terminal state without a refetch.
    * @param payload Transpiled from Source.Features.RuntimeCuration.Models.RuntimeProposalUpdatedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeProposalUpdated(payload: RuntimeProposalUpdatedPayload): Promise<void>;
    /**
    * Pushed when a runtime's daemon reports a disk-pressure transition
    * (Phase D Card 3). One push per transition — the daemon emits only on
    * level changes, not on every sample, so this is a rare event under
    * normal operations. Drives the project dashboard's disk-warning banner.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.RuntimeDiskPressureNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeDiskPressure(payload: RuntimeDiskPressureNotification): Promise<void>;
    /**
    * Pushed when the daemon's canUseTool callback fires and a human
    * needs to approve (or deny) a single tool invocation. Correlation by
    * SDK toolUseId on the payload — the eventual
    * AgentHub.ResolvePermission echoes the same id back so the
    * daemon can match the decision to its in-flight callback. Drives the
    * inline approval card the spec describes in ChatCanvas.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.PermissionRequestedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    permissionRequested(payload: PermissionRequestedPayload): Promise<void>;
    /**
    * Pushed when the daemon emits a structured runtime event (bootstrap
    * stage, install / setup step, supervised service lifecycle, spec delta
    * apply / fail — see RuntimeEventTypes) and the backend has
    * successfully appended it to the event store. Frontends opt in to this
    * channel by calling AgentHub.SubscribeToRuntimeEvents(runtimeId)
    * on drawer mount; the broadcast lands in the
    * runtime-events:{runtimeId} group. Drives the live Timeline tab.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.RuntimeEventNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    runtimeEventReceived(payload: RuntimeEventNotification): Promise<void>;
    /**
    * Pushed for every line the daemon tails out of a supervised service's
    * log file. Drives the live "Logs" tab in the runtime drawer. Subscribers
    * opt in by calling AgentHub.SubscribeToServiceLogs(runtimeId, serviceName)
    * — the broadcast lands in the
    * service-logs:{runtimeId}:{serviceName} group. Not persisted
    * server-side; the daemon tails from disk on demand.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ServiceLogLineNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    serviceLogLine(payload: ServiceLogLineNotification): Promise<void>;
    /**
    * Pushed every time the daemon's ServiceStatusPoller samples a
    * fresh supervisord process list (default once every 10s). Drives the
    * live "Services" tab in the super-admin runtime drawer so transient
    * states (FATAL / BACKOFF / STOPPED) the event-driven Timeline can't
    * represent end-to-end are always visible. Subscribers are the same
    * runtime-events:{runtimeId} group that already receives
    * .
    * @param payload Transpiled from Source.Features.SignalR.Contracts.LiveSupervisordSnapshotNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    liveSupervisordSnapshotReceived(payload: LiveSupervisordSnapshotNotification): Promise<void>;
    /**
    * runtime-observability-super-admin — pushed for every line the daemon
    * tails out of /var/log/supervisor/agent.out.log +
    * agent.err.log. Drives the super-admin runtime drawer's
    * "Daemon Logs" tab. Subscribers opt in by calling
    * AgentHub.SubscribeToDaemonLogs(runtimeId) — the broadcast lands
    * in the daemon-logs:{runtimeId} group. Not persisted server-side;
    * the daemon tails from disk on demand.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.DaemonLogLineNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    daemonLogLineReceived(payload: DaemonLogLineNotification): Promise<void>;
    /**
    * Pushed whenever a project's preview port is hot-swapped through
    * PATCH /api/projects/{projectId}/preview-port. Fans out to every
    * branch-{branchId} group of the project AND the parent
    * workspace-{workspaceId} group so settings dialogs / sidebars /
    * runtime drawers can re-render the port live. The Cloudflare tunnels
    * have already been re-pointed at the new port before this broadcast
    * fires — receivers can trust the value is live at the edge.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.PreviewPortChangedNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    previewPortChanged(payload: PreviewPortChangedNotification): Promise<void>;
    /**
    * chat-file-attachments — pushed whenever the daemon acks a staging
    * attempt (success or failure) for an attachment in this branch's
    * composer. Drives the per-chip state machine: "Ready" flips
    * "Staging on runtime…" → checkmark, "Failed" flips it to
    * "Runtime download failed" with Retry/Remove. Fan-out scope is
    * branch-{branchId} so sibling-branch tabs don't see each other's
    * composer events.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.AttachmentStateChangedPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    attachmentStateChanged(payload: AttachmentStateChangedPayload): Promise<void>;
}

/**
* Strongly-typed receiver interface for clients connected to .
* Every method here is invoked by the server on the React client; the React side
* registers handlers for these names. Keeping the contract on a single interface
* means a backend change that drops a method breaks compile in the generated
* TypeScript — same convention as .
* 
* Payloads are intentionally payload-light. The frontend re-fetches via
* Orval after invalidation; the notification's job is "tell me something changed
* and what to refetch", not "transport the new state". Keeps the wire small
* and avoids two sources of truth for entity shape.
*/
export type IPlanningClient = {
    /**
    * Pushed when a Specification is created / updated / deleted under
    * a project the connection is subscribed to (via
    * PlanningHub.JoinProject). Drives the live spec list / detail
    * without polling.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.SpecificationChangedNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    specificationChanged(payload: SpecificationChangedNotification): Promise<void>;
    /**
    * Pushed for every lifecycle transition on a ProjectKanbanCard —
    * create, edit, move (status / position), soft-delete. Drives the live
    * kanban board re-render in every open tab on the project.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.CardChangedNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    cardChanged(payload: CardChangedNotification): Promise<void>;
    /**
    * Pushed for every lifecycle transition on a ProjectKanbanCardSubtask —
    * create, toggle (complete / incomplete), soft-delete. Routed by the
    * parent card's ProjectId so subscribers join one group per
    * project, not per card.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.SubtaskChangedNotification
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    subtaskChanged(payload: SubtaskChangedNotification): Promise<void>;
}

/**
* Strongly-typed receiver interface for daemons connected to .
* Every method here is invoked by the server on the daemon; the daemon registers
* handlers for these names. The full payload shapes for these calls ship in
* dependent specs (daemon-architecture, runtime-bootstrap) — this card only
* fixes the method names so the typed-hub contract compiles.
*/
export type IRuntimeClient = {
    /**
    * Server-to-daemon: begin a new agent turn for the supplied session.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.StartTurnPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    startTurn(payload: StartTurnPayload): Promise<void>;
    /**
    * Server-to-daemon: abort the in-flight turn.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.CancelTurnPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    cancelTurn(payload: CancelTurnPayload): Promise<void>;
    /**
    * Server-to-daemon: install an additive runtime-spec delta after a user
    * approved (or edited) a . The daemon shells
    * out to mise + supervisord idempotently and acks via
    * ; the proposal id
    * in the payload is the correlation key. Additive only — entries already
    * present in ProjectRuntime.Spec are stripped server-side.
    * @param payload Transpiled from Source.Features.RuntimeCuration.Models.ApplyRuntimeSpecDeltaPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    applyRuntimeSpecDelta(payload: ApplyRuntimeSpecDeltaPayload): Promise<void>;
    /**
    * Server-to-daemon: hot-apply a config refresh (model, thresholds, flags).
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ConfigUpdatePayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    updateConfig(payload: ConfigUpdatePayload): Promise<void>;
    /**
    * Server-to-daemon: a user (or admin) approved a destructive git operation
    * previously requested by the daemon. The daemon looks up its in-memory
    * approval record by  and proceeds with the
    * reset / force-push / branch-delete that was held pending. Fan-out only;
    * the audit row already exists from the daemon's
    * RequestDestructiveGitOp roundtrip.
    * @param opId Transpiled from System.Guid
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    executeDestructiveGitOp(opId: string): Promise<void>;
    /**
    * Server-to-daemon: a user has asked the runtime to merge a branch. The
    * daemon runs the merge and reports back through the standard git-op
    * fan-out (started/completed, plus merge-conflict on conflicts).
    * @param payload Transpiled from Source.Features.GitOps.Models.MergeBranchPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    mergeBranch(payload: MergeBranchPayload): Promise<void>;
    /**
    * Server-to-daemon: restart a single managed service (Phase D Card 2).
    * Triggered by the heartbeat handler's service-down detector when the
    * runtime spec lists a service the daemon reports as not running.
    * Throttled at most 3 per 5 minutes per (runtimeId, serviceName)
    * — see  for the full
    * trigger / throttle rationale.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.RestartServicePayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    restartService(payload: RestartServicePayload): Promise<void>;
    /**
    * Server-to-daemon: wipe local bootstrap state and re-run the bootstrap
    * flow against a freshly-fetched bundle. Pushed by the admin
    * force-rebootstrap endpoint when an operator needs to recover a stuck
    * or partially-bootstrapped runtime without a full Fly machine recreate.
    * See  for the trigger and
    * audit-trail rationale.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ForceRebootstrapPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    forceRebootstrap(payload: ForceRebootstrapPayload): Promise<void>;
    /**
    * Server-to-daemon: the user has answered a previously-requested
    * permission prompt. Correlated by the payload's ToolUseId — the
    * daemon looks up its in-memory pending canUseTool waiter by that
    * key and resolves it with the supplied decision (+ optional feedback for
    * the "deny with feedback" branch). Fan-out is targeted at the single
    * daemon connection currently bound to the project's active runtime; no
    * broadcast.
    * @param payload Transpiled from Source.Features.SignalR.Contracts.ResolvePermissionPayload
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    permissionResolved(payload: ResolvePermissionPayload): Promise<void>;
    /**
    * Server-to-daemon: a frontend opened the Logs tab and wants live tail of
    * a supervised service's stdout/stderr. Pushed by
    * AgentHub.SubscribeToServiceLogs after it has validated the
    * service name against the runtime's current spec and added the caller's
    * connection to the service-logs:{runtimeId}:{serviceName} group.
    * 
    * The daemon's LogTailer reference-counts subscriptions — a
    * subsequent call for the same serviceName increments the count
    * instead of spawning a second tail -F. The daemon emits log lines
    * back via RuntimeHub.ServiceLogLine.
    * @param serviceName Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    startLogTail(serviceName: string): Promise<void>;
    /**
    * Server-to-daemon: a frontend closed (or unmounted) the Logs tab. The
    * daemon decrements its LogTailer ref-count for the service; when
    * it reaches zero the tail process is SIGTERM'd. Symmetric counterpart to
    * .
    * @param serviceName Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    stopLogTail(serviceName: string): Promise<void>;
    /**
    * runtime-observability-super-admin — server→daemon: a super-admin
    * opened the runtime drawer's Daemon Logs tab and wants live tail of the
    * daemon's own stdout/stderr. Pushed by
    * AgentHub.SubscribeToDaemonLogs after the super-admin gate
    * passes. The daemon tails
    * /var/log/supervisor/agent.out.log + agent.err.log via its
    * LogTailer (with initialLines: 200 so the subscriber sees
    * recent context immediately) and emits lines back via
    * RuntimeHub.DaemonLogLine. Reference-counted on the daemon side
    * — subsequent calls just bump the count.
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    startDaemonLogTail(): Promise<void>;
    /**
    * runtime-observability-super-admin — server→daemon: a super-admin
    * closed (or unmounted) the runtime drawer's Daemon Logs tab. The
    * daemon decrements its daemon-log LogTailer ref-count; on zero
    * the underlying tail -F processes are SIGTERM'd. Symmetric
    * counterpart to .
    * @returns Transpiled from System.Threading.Tasks.Task
    */
    stopDaemonLogTail(): Promise<void>;
    /**
    * Server→daemon (request/response): list the files that differ in the
    * requested scope. Phase 1 of the diff-view-tab spec only supports
    * workingTree; branch / commit / range scopes (with base /
    * head fields on the request) ship in Phase 3 alongside the picker
    * UI. The daemon serialises this through GitModule's queue so it
    * can't race auto-commit / rebase work in flight, then shells out to
    * git status --porcelain=v2 + git diff --numstat HEAD for
    * the working-tree case (see DiffQueries.ts on the daemon).
    * 
    * Caps live on the daemon side: 5000 files max in
    * ; over the cap the array is
    * truncated and  set to
    * "too-many".
    * @param runtimeId Transpiled from string
    * @param req Transpiled from Source.Features.Diffs.ChangedFilesRequest
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.Diffs.ChangedFilesResponse>
    */
    getChangedFiles(runtimeId: string, req: ChangedFilesRequest): Promise<ChangedFilesResponse>;
    /**
    * Server→daemon (request/response): unified-diff text for a single file
    * in the requested scope. Tracked working-tree files use
    * git diff -U3 HEAD -- <path>; untracked files fall back to
    * git diff -U3 --no-index /dev/null <path>. Always run with
    * -c diff.renames=true -c core.quotepath=false so we get rename
    * detection and unicode paths verbatim.
    * 
    * Body capped at 500 KB; over the cap the head slice is returned
    * with  true. Binary files
    * return with no body,  true,
    * = "binary".
    * @param runtimeId Transpiled from string
    * @param req Transpiled from Source.Features.Diffs.FileDiffRequest
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.Diffs.FileDiffResponse>
    */
    getFileDiff(runtimeId: string, req: FileDiffRequest): Promise<FileDiffResponse>;
    /**
    * Server→daemon (request/response, Phase 3): branch-scope changed-files
    * query. Compares ..
    * — typically main..HEAD for the new default UX. The daemon
    * validates both refs with git rev-parse --verify before running
    * the diff and surfaces a typed error when either can't be resolved
    * (the controller turns that into a 400). Same wire shape as
    * so the React layer can swap between
    * scopes without an adapter.
    * @param runtimeId Transpiled from string
    * @param baseRef Transpiled from string
    * @param headRef Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.Diffs.ChangedFilesResponse>
    */
    getBranchChangedFiles(runtimeId: string, baseRef: string, headRef: string): Promise<ChangedFilesResponse>;
    /**
    * Server→daemon (request/response, Phase 3): single-file unified diff
    * between two refs. Same body-clipping / binary-detection rules as
    * ; same ref-not-found surfacing as
    * .
    * @param runtimeId Transpiled from string
    * @param baseRef Transpiled from string
    * @param headRef Transpiled from string
    * @param path Transpiled from string
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.Diffs.FileDiffResponse>
    */
    getBranchFileDiff(runtimeId: string, baseRef: string, headRef: string, path: string): Promise<FileDiffResponse>;
    /**
    * Server→daemon (request/response, Phase 3): newest-first list of
    * commits in ...
    * Drives the commit-picker UI.  caps the
    * returned row count (default 200 on the daemon; hard cap 1000).
    * Throws when either ref is invalid.
    * @param runtimeId Transpiled from string
    * @param baseRef Transpiled from string
    * @param headRef Transpiled from string
    * @param limit Transpiled from int
    * @returns Transpiled from System.Threading.Tasks.Task<Source.Features.Diffs.CommitRangeResponse>
    */
    getCommitRange(runtimeId: string, baseRef: string, headRef: string, limit: number): Promise<CommitRangeResponse>;
}

